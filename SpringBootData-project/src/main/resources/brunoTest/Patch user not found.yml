info:
  name: Patch user not found
  type: http
  seq: 15

http:
  method: PATCH
  url: http://localhost:8080/users/patch/4
  body:
    type: json
    data: |-
      {
        "firstName": "Laura",
        "lastName": "Muresan",
        "email": "laura_muresan@gmail.com",
        "salary": 117.00,
        "password":"qwerty45"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
