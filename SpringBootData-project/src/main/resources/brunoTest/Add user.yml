info:
  name: Add user
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/users/add
  body:
    type: json
    data: |-
      {
        "firstName": "Laura",
        "lastName": "Muresan",
        "email": "laura_muresan@gmail.com",
        "salary": 17.00,
        "password":"qwerty45"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
